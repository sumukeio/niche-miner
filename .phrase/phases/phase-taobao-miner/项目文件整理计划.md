# 项目文件整理计划

## 当前问题

1. **根目录混乱**：测试文件、Python脚本、文档混在一起
2. **文件分散**：Python脚本在根目录，应该统一管理
3. **测试文件未管理**：Excel测试文件、截图文件散落
4. **目录结构不规范**：缺少统一的组织方式

## 整理方案

### 目录结构

```
niche-miner/
├── .phrase/                    # 文档驱动开发（DDD）根目录
│   ├── docs/                   # 项目文档
│   └── phases/                 # 各阶段的文档
│       ├── phase-init-20260103/
│       └── phase-taobao-miner/
│
├── scripts/                    # Python 脚本（新增）
│   ├── baidu_ad_validator.py
│   ├── taobao_miner.py
│   ├── screenshots/            # 截图目录（移动）
│   └── README.md               # 脚本使用说明
│
├── samples/                    # 示例/测试文件（新增）
│   ├── keywords.xlsx           # 示例关键词文件
│   └── keywords_validated.xlsx # 示例验证结果
│
├── src/                        # Next.js 源代码
│   ├── app/
│   ├── lib/
│   └── types.d.ts
│
├── public/                     # 静态资源
├── temp/                       # 临时文件（已存在，gitignore）
│
├── .cursorrules               # Cursor 规则
├── .gitignore
├── package.json
├── requirements.txt            # Python 依赖
├── README.md                   # 项目主 README
└── tsconfig.json
```

### 文件移动清单

1. **Python 脚本**
   - `baidu_ad_validator.py` → `scripts/baidu_ad_validator.py`
   - `taobao_miner.py` → `scripts/taobao_miner.py`

2. **截图文件**
   - `screenshots/` → `scripts/screenshots/`

3. **测试/示例文件**
   - `keywords.xlsx` → `samples/keywords.xlsx`
   - `keywords_validated.xlsx` → `samples/keywords_validated.xlsx`
   - `README_BAIDU_VALIDATOR.md` → `scripts/README.md`（合并或移动）

4. **清理**
   - 删除 `__pycache__/`
   - 修复 `src/app/project/[id\]` 目录结构问题

### 更新内容

1. **更新 .gitignore**
   - 添加 `samples/*.xlsx`（示例文件可以提交）
   - 添加 `scripts/screenshots/*.png`（截图可以提交，或者不提交）
   - 添加 `__pycache__/`
   - 添加 `*.pyc`

2. **更新 README.md**
   - 说明目录结构
   - 更新脚本路径

3. **更新 API 路由中的脚本路径**
   - `src/app/api/baidu-validator/route.ts`
   - `src/app/api/taobao-miner/*/route.ts`





